    if [ $1 ] #If argument 1 is given
    then
    echo "$1" #Say that number
    else #If no argument was given
    echo "No arguments given" #Say that
    fi #Finish

    if [ $2 ] #If argument 2 is given
    then
    echo "$2" #Say that number
    fi #Finish

    if [ $3 ] #If argument 3 is given
    then
    echo "$3" #Say that number
    fi #Finish
